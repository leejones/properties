<table>
  <tr>
    <th>Photo</th>
    <th>Rent</th>
    <th>BR</th>
    <th>BA</th>
    <th>Address</th>
    <th>Type</th>
    <th colspan="3">Actions</th>
  </tr>

<% properties.each do |property| %>
  <tr>
    <td>
      <% if property.photos %>
        <%= image_tag property.photos.first.url, :alt => property.photos.first.caption, :width => 75, :style => "padding:0px" %>
      <% end %>
    </td>
    <td><%=h number_to_currency(property.price) %></td>
    <td><%=h property.bedrooms %></td>
    <td><%=h property.bathrooms %></td>
    <td><%=h truncate(property.street) %>, <%=h property.city %></td>
    <td><%=h truncate(property.property_type.name) if property.property_type %></td>
    <td><%= link_to 'Show', property %></td>
    <td><%= link_to 'Edit', edit_property_path(property) %></td>
    <td><%= link_to 'Destroy', property, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
